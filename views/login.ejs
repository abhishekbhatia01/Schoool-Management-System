<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body{ 
            font-family: Arial, Helvetica, sans-serif;
            text-align: center; 
            padding: 50px; 
            background-color: white;
            
        }
        .container { 
            width: 700px; 
            height: 400px;
            margin: auto; 
            /* padding: 20px;  */
            border: 1px solid #ddd; 
            border-radius: 5px; 
            background-color: white;
            box-shadow: rgba(26, 19, 19, 0.35) 0px 5px 15px;
            background-image: url(/images/pexels-tirachard-kumtanom-112571-733852.jpg);
            background-repeat: no-repeat;
            background-size: cover;
           
        }
        .container h1{
            color: #3D3D3D;
        }
        input { 
            width: auto ; 
            padding: 10px ; 
            /* padding-right: 300px; */
            width:400px;
            margin: 40px 0 0 0; 
            border: none;
            box-shadow: rgba(0, 0, 0, 0.1) 0px 4px 12px;
        }

        @media(max-width:575px){
            input{
                width:300px;
            }
        }

        @media(max-width:475px){
            input{
                width:200px;
            }
        }

        @media(max-width:375px){
            input{
                width:100px;
            }
        }

        input:focus {
            outline: none; /* Ensures no outline appears on focus */
            border-color: #121a22; /* Optional: Changes border color on focus */
            box-shadow: 0 0 5px rgba(25, 32, 39, 0.5); /* Optional: Adds a subtle glow */
}
        .error { 
            color: red; 
        }
        button{
            padding:13px 35px;
            font-size: large;
            margin-top: 50px;
            background-color: #007BFF;
            border: none;
            border-radius: 7px;
            color: white;
        }
        button:hover{
            cursor: pointer;
        }
        a{
            text-decoration: none;
            color: #091057;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Login</h1>
        <p class="error"><%= message %></p>
        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="Username" required><br>
            <input type="password" name="password" placeholder="Password" required><br>
            <button type="submit">Login</button>
        </form>
        <p>Don't have an account? <a href="/signup">Signup</a></p>
    </div>
</body>
</html>
